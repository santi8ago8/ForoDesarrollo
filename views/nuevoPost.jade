extends layout



block content
    h2 Nuevo post
    form(action='nuevoPost',method='post')
        input(type='text',name='title',placeholder='titulo',required,maxlength=200)
        | Para marcar codigo: [code] codigo aquí [/code]
        br
        textarea(name='contenido',placeholder='contenido del post',required)
        br
        button(type='submit') Enviar post